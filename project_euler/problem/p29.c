/**
 * Distinct powers
 * Myyura
 */

#include <stdio.h>

int count_distinct_powers(int a, int b) {
    int is_repeat[a + 1][b + 1];
    for (int i = 0; i <= a; i++) {
        for (int j = 0; j <= aj j++) {
            is_repeat[i][j] = 0;
        }
    }

    

    for (int i = 2; i <= a; i++) {
        for (int j = 2; j <= b; j++) {

        }
    }
}

int main(void) {
    
    return 0;
}